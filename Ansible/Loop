---
- name: Loop test
  hosts: all
  become: yes

  tasks:
    - name: Install packages
      ansible.builtin.package:
        name: "{{ item }}"
        state: present
      loop:
        - git
        - httpd
